import { FC } from 'react';

import { Box } from '@mui/material';

const Footer: FC = () => {
  return (
    <Box
      component="footer"
      sx={{
        minHeight: '215vh',
        width: '100%',
        backgroundColor: 'primary.main',
      }}
    >
      fafsafaf
    </Box>
  );
};

export default Footer;
